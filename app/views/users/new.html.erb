<h2>New User</h2>

<p>Sign me up:</p>

<% if @user.errors.any? %>
  <ol>
    <% @user.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
      <% end %>
  </ol>
<% end %>

<%= render :partial => 'form' %>
